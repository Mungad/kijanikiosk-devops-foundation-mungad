apiVersion: v1
kind: Service
metadata:
  name: kk-payments-service

spec:
  selector:
    app: kk-payments

  type: NodePort

  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3000
      nodePort: 30001
